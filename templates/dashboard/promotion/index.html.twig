{% extends 'dashboard.html.twig' %}

{% block form %} 
					<h4> <small><a href="/dashboard/profile/promotion/add" >Create New</a></small></h4>                    
				         <table id="missing-persons" class="table table-striped table-td-valign-middle">
								<thead>
									<tr>
										<th class="text-nowrap">Name</th>
										<th class="text-nowrap">Amount</th>
										<th class="text-nowrap">Type</th>
										<th class="text-nowrap">Start date</th>
										<th class="text-nowrap">End date</th>
										<th class="text-nowrap">Action</th>
									</tr>
								</thead>
								<tbody>
										{% for item in list %} 
										<tr  class="" >
											<td><a href="/dashboard/profile/promotion/add/{{item.id}}" >{{item.name|length > 15 ? item.name|slice(0, 15) ~ '...' : item.name}}</a></td>
											<td>{{item.amount}}</td>
                                            <td>{{item.type}}</td>
											<td>{{item.startDate}}</td>	
											<td>{{item.endDate}}</td>											
											<td>											
											<a href="/dashboard/profile/promotion/delete/{{item.id}}" >Delete <span class="fa fa-trash text-success"></span></a></td>
										</tr>
										{% endfor %}
										</tbody>
				  			</table>                        							
						</div>
                  </div>
      </section>
{% endblock %}
