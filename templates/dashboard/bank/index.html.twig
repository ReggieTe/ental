{% extends 'dashboard.html.twig' %}

{% block form %} 
					<h4> <small><a href="/dashboard/profile/account/add" >Create New</a></small></h4>                    
				         <table id="missing-persons" class="table table-striped table-td-valign-middle">
								<thead>
									<tr>
										<th class="text-nowrap">Holder</th>
										<th class="text-nowrap">Number</th>
										<th class="text-nowrap">Bank</th>
										<th class="text-nowrap">Type</th>
										<th class="text-nowrap">Action</th>
									</tr>
								</thead>
								<tbody>
										{% for item in list %} 
										<tr  class="" >
											<td><a href="/dashboard/profile/account/add/{{item.id}}" >{{item.holder|length > 15 ? item.holder|slice(0, 15) ~ '...' : item.holder}}</a></td>
											<td>{{item.account}}</td>
                                            <td>{{item.bank}}</td>
											<td>{{item.type}}</td>											
											<td>											
											<a href="/dashboard/profile/account/delete/{{item.id}}" >Delete <span class="fa fa-trash text-success"></span></a></td>
										</tr>
										{% endfor %}
										</tbody>
				  			</table>                        							
						</div>
                  </div>
      </section>
{% endblock %}
